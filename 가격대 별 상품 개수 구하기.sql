SELECT 
CASE WHEN PRICE < 10000 THEN 0
     WHEN PRICE >= 10000 then FLOOR(PRICE/10000) * 10000
     end as PRICE_GROUP,
     COUNT(*) as PRODUCTS
from product
GROUP BY PRICE_GROUP
ORDER BY PRICE_GROUP ASC;